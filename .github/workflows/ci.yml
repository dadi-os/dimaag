name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: dimaag-ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_USER: dimaag
          POSTGRES_PASSWORD: dimaag
          POSTGRES_DB: dimaag
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U dimaag -d dimaag"
          --health-interval 5s --health-timeout 5s --health-retries 10

    steps:
      - uses: actions/checkout@v6

      - name: Build dev image
        run: docker build --target dev -t dimaag-dev .

      - name: Migrate
        run: |
          docker run --rm --network host \
            -e DATABASE_URL=postgres://dimaag:dimaag@localhost:5432/dimaag \
            -v "$PWD:/app" -v /app/node_modules \
            dimaag-dev npm run db:migrate

      - name: Test
        run: |
          docker run --rm --network host \
            -e DATABASE_URL=postgres://dimaag:dimaag@localhost:5432/dimaag \
            -v "$PWD:/app" -v /app/node_modules \
            dimaag-dev npm test

      - name: Build production image
        run: docker build --target production -t dimaag-prod .

  publish:
    needs: ci
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          target: production
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/dimaag:latest
            ghcr.io/${{ github.repository_owner }}/dimaag:${{ github.sha }}
